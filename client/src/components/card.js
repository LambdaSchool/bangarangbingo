import React from 'react';
import SideNav from './sidenav';

const Card = () => (
  <div>
    <SideNav />
    <div className="cards">
      SINGLE CARD CREATE AND EDIT PAGE
    </div>
  </div>
);

export default Card;
